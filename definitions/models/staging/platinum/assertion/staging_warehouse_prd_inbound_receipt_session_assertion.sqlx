
-- config { type: "assertion" }

-- with count_silver as
-- (
--   SELECT
--     count(mg_id)
--     --mg_id
--    FROM (
--          SELECT
--              mg_id, action, synced_at, ingest_time, last_updated_time, created_time
--          FROM
--              ${source.silver}.warehouse_prd_inbound_receipt_session
--          where ingest_time <= (select max(ingest_time) as max_ingested_time from ${ref("warehouse_prd_inbound_receipt_session")})
--          QUALIFY ROW_NUMBER() OVER(_window) = 1
--              AND FIRST_VALUE(action) OVER(_window) <> "d"
--          WINDOW
--              _window AS (
--                      PARTITION
--                          BY
--                          mg_id
--                      ORDER
--                          BY
--                          synced_at
--                          DESC,
--                          IF(action='d',3,IF(action='u',2,1)) DESC, last_updated_time DESC))
--     where ((created_time is not null and last_updated_time is not null)
--             OR (created_time IS NULL AND last_updated_time IS NOT NULL) OR (created_time IS NOT NULL AND last_updated_time IS NULL)) or action = "i" -- delete some record that have action = "u" but already deleted in mongo
-- )

-- select 1 as check_equal from
-- (select count(mg_id) as count_distinct_platinum from ${ref("warehouse_prd_inbound_receipt_session")})
-- where count_distinct_platinum != (select * from count_silver)
