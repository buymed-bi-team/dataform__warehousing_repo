config {
    type: "table", 
    description: 
        `
        Active Tags on Thuocsi;
        PIC: nguyen.tran; 
        Type: Table; 
        Refresh Schedule: hourly;
        `,
    tags: ["custom_1h_past5"]
}

SELECT
    *
FROM ${ref("dim_sku_tag")}
WHERE tag_code = 'LNC' OR type = 'THUOCSI'