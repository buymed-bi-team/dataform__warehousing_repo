config {
    type: "view",
    name: "tech__agg_seller_oos",
    schema: "buymed_tech",
    // tags: ["custom_1h_past5"],
}

SELECT * 
FROM ${ref(`agg_seller_oos`)}
WHERE 
    (qty_incoming = 0 OR qty_incoming IS NULL)
    AND (qty_available = 0 OR qty_available IS NULL)
    