config {
    type: 'view',
    schema: "buymed_innovation",
    description: `Inventory of Circa.`,
    tags: ['AI_CIRCA']
}

SELECT * EXCEPT(session_created_at, session_created_date)
FROM ${ref('gold_buymed_vn2','circa_chat_sessions')}
WHERE message_count > 0 