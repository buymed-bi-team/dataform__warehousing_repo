config {
    type:"operations"
}

-- fact_order_item

CREATE OR REPLACE VIEW `rumi_buymed_cam.thuocsi_cam_fact_order_item`
AS (SELECT * FROM  buymed_innovation.thuocsi_cam_fact_order_item);


-- fact_margin

CREATE OR REPLACE VIEW `rumi_buymed_cam.thuocsi_cam_fact_margin`
AS (SELECT * FROM  buymed_innovation.thuocsi_cam_fact_margin);

-- dim_customer

CREATE OR REPLACE VIEW `rumi_buymed_cam.thuocsi_cam_dim_customer`
AS (SELECT * FROM  buymed_innovation.thuocsi_cam_dim_customer);

-- dim_product_sku

CREATE OR REPLACE VIEW `rumi_buymed_cam.thuocsi_cam_dim_product_sku`
AS (SELECT * FROM  buymed_innovation.thuocsi_cam_dim_product_sku);



