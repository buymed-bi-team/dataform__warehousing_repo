config {
    type:"operations",
    description: `PIC: minh.luu; Schedule: 0h00 daily`,
    tags:['0h00']
}

-- Create description for agg_strategic_general
js {
    function current_date() {
        var d = new Date();
        var strDate = d.getFullYear().toString() + ("0" + (d.getMonth() + 1)).slice(-2) + ("0" + (d.getDate())).slice(-2);
        return strDate
    }
    const today = current_date()
}

CREATE SNAPSHOT TABLE ${source.ref}.agg_strategic_general_${today}
CLONE ${ref(`agg_strategic_general`)}
-- FOR SYSTEM_TIME AS OF
--   TIMESTAMP_SUB(CURRENT_TIMESTAMP(), INTERVAL 1 HOUR)
OPTIONS (expiration_timestamp = TIMESTAMP_ADD(CURRENT_TIMESTAMP(), INTERVAL 30 DAY));